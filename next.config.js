/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  // basePath: "/demo",  
  // assetPrefix: "/demo/",
};

module.exports = nextConfig;
